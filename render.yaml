services:
  - type: web
    name: la-primitiva-proxy
    runtime: python
    buildCommand: pip install -r proxy/requirements.txt
    startCommand: cd proxy && uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
